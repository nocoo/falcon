# Scenario tests. force_try, optional_data_string_conversion, cyclomatic_complexity,
# and large_tuple stay at the recommended preset. Style budget only.

file_length:
  warning: 700
  error: 1000
  ignore_comment_only_lines: true

function_body_length:
  warning: 100
  error: 160
